sequenceDiagram
participant browser
participant server

    Note right of browser: User writes a note into the text field
    Note right of browser: User clicks the Save button

    Note right of browser: Browser executes JavaScript event handler
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/spa/notes
    activate server
    Note right of server: Server saves the new note
    server-->>browser: JSON of the saved note
    deactivate server

    Note right of browser: Browser receives JSON
    Note right of browser: Browser updates the DOM to show the new note
